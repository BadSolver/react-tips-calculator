import styled from "styled-components";
import shape from "../assets/circle.svg";

export const StyledWrapper = styled.div`
  max-width: 1510px;
  background: no-repeat url(${shape}) #eaf2f2;
  margin: 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;
`;
