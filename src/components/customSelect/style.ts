export const customStyles = {
    control: (styles: any) => ({
        ...styles,
        marginTop: '30px',
        textAlign: 'center',
        color: '#756C6C',
        borderRadius: '30px',
        paddingTop: '15px',
        paddingBottom: '15px',
    }),
    indicatorSeparator: (styles: any) => ({
        ...styles,
        display: 'none',
    })
}